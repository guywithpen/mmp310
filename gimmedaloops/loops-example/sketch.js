

function setup() {
    createCanvas (windowWidth, windowHeight);
    background (51);
    
    if (0 < 1) {
        ellipse(width/2, height/2, 200);
    }
    
    for (let i = 0; i < 5; i++) {
        console .log('i is', i);
    }
 
}

